{
  "name": "noc-thoughts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/build-sitemap.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "validate": "npm run build && node scripts/validate-site.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "astro": "7.0.8"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.6",
    "sharp": "0.35.3",
    "typescript": "5.9.3"
  }
}
