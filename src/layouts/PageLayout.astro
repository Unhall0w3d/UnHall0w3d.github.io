---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title: string;
  description?: string;
  eyebrow?: string;
  robots?: string;
}

const { title, description, eyebrow = "NOC THOUGHTS // REFERENCE NODE", robots } = Astro.props;
---

<BaseLayout title={title} description={description} robots={robots}>
  <header class="page-hero">
    <p class="eyebrow">{eyebrow}</p>
    <h1>{title}</h1>
    {description && <p class="page-deck">{description}</p>}
  </header>
  <div class="page-shell prose">
    <slot />
  </div>
</BaseLayout>
